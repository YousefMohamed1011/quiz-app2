# Quiz App

تطبيق React مبني باستخدام Vite وJavaScript.

## التشغيل محليًا

```bash
npm install
npm run dev
```

## الأوامر المتاحة

- `npm run dev` لتشغيل بيئة التطوير.
- `npm run build` لإنشاء نسخة الإنتاج داخل `dist`.
- `npm run lint` لفحص الملفات.
